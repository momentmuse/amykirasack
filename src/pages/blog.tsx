import * as React from 'react';
import Layout from './../components/layout';

const Blog = () => {
  return (
    <Layout>
      <h1>This is the Blog page 📝 </h1>
    </Layout>
  );
};

export default Blog;
